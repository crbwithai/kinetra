// Branş duvarı ve derin vitrin kartlarının paylaştığı giriş animasyonu — tek kaynak,
// "aynı desen" iki yerde ayrı ayrı yazılmasın diye.
export const FADE_UP = {
  hidden: { opacity: 0, y: 16 },
  visible: { opacity: 1, y: 0 },
}
